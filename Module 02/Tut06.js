"use strict";

// DOM: Document Object Model

// Brwoser parse all HTML content and create an Object, and put in in global object: window as its one of property

// Easy representation of an object for HTML

// NOTE: Merely every return element is an Object here, with their some properties

console.log(window.document);

// Actual representation of an object
console.dir(window);