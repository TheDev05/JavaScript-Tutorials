"use strict";

const header = document.querySelector("h2");

const val = document.createElement("h2");
val.textContent = "Your Logo";

// setattribute(x,y) --> takes two parameter, first which one to be changed. second --> to what atribute is x get changed

// removeatribute(x) --> simply remove attribute, takes attribute name
